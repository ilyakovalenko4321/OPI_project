spring:
    liquibase:
        change-log: classpath*:liquibase/db.changelog.yaml
        enabled: true
        default-schema: public

    datasource:
        url: jdbc:postgresql://localhost:5432/opi_first_progect?currentSchema=public
        username: postgres
        password: 12344578
        driver-class-name: org.postgresql.Driver